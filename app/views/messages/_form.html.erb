<%= form_with model: [plant, chat, message] do |f| %>
  <div class="input-group">
    <%= f.text_area :content, class: "form-control", placeholder: "Ask about your plant...", rows: 2 %>
    <%= f.submit "Send", class: "btn btn-success" %>
  </div>
<% end %>
